# PROMETHEUS PRE-COMMITMENT BRIEF: MONDAY ROUTING PROTOCOL
**Status**: SEALED. To be executed unconditionally post-experiment on Monday.
**Core Inputs Required**: 
1) Joint Classifier Accuracy % (and `n` trades)
2) Amihud Q1 Autocorr on 30-min
3) Cat 4 split (Score 0 vs Score 2)

---

### BRANCH 1: CONFIRMED
**Trigger Condition**: Joint Classifier hits `≥ 58.0%` precision, with sample size `n ≥ 40`.
**Interpretation**: The hypothesis is structurally sound across multiple substrates and has survived out-of-sample forward verification. The CV-adjusted prior of 52-57% was successfully pierced by genuine predictive drift powered by institutional repositioning logic (Cat 4 conflict). The anomaly is real, stable, and ready to scale.
**Next Action**: PROCEED to complete the 6-month retrospective futures pull, maintain continuous paper validation, and schedule micro-live ₹50K deployments once initial real-time forward validation samples complete.

### BRANCH 2: BORDERLINE
**Trigger Condition**: Joint Classifier output lands inside the strict bounds of `53.0%` to `57.9%` with `n ≥ 40`. 
**Interpretation**: The system performs correctly above the algorithmic 50% noise baseline but suffers from insufficient statistical velocity to warrant direct capital risk immediately. The anomaly exists but remains clouded within the stochastic margin of error on the limited 60-day constraint; the OOS expectation holds but remains mathematically unconfirmed for risk deployment. 
**Next Action**: EXTEND the research horizon mechanically. Expand historical scraping to 6 months of futures data directly to bolster `n` and enforce reliance entirely on accumulating the raw live paper-trading sample to prove independent out-of-sample forward superiority. Delay all capital deployments.

### BRANCH 3: REJECTED
**Trigger Condition**: Joint Classifier evaluates at `< 53.0%`.
**Interpretation**: The 10:45–11:15 AM intraday phenomenon fails to clear the baseline hurdle on pure futures substrate. The robust cross-validation expectations ultimately dissolved out-of-sample, exposing the limits to price-only temporal compression signals. The hypothesis is definitively falsified on forward predictive capacity and must not be optimized further or revisited.
**Next Action**: PIVOT the directional research entirely toward the untested Substrates B and F (OI/Volume and Options Flow) on the newly obtained futures dataset. Discard the intraday module and elevate the structural VRP options protocol (+3.56 pts, 80% hit-rate) to the absolute primary focus for capital pathway generation.

## Expected Value & Profit Target Expectations

The primary mechanism for generating sustained edge is a probabilistic outcome matrix applied strictly, rather than seeking guaranteed wins on every trade. The following defines the mathematical basis for capital Scenario A (?15,000 Micro).

**System Mathematical Model:**
- Win Rate Parameter: 55%
- Loss Rate Parameter: 45%
- Profit per winning trade: ?300 
- Loss per losing trade: ?150 
- Total edge generated over time per trade: = (0.55 � 300) - (0.45 � 150) = +?97.50

The strategy explicitly abandons the goal of earning ?300 on every single trade. It aims for a structural yield of ?97.50 expected value (EV) per deployed trade, scaling dynamically with account size in higher tiers.
